{
  "name": "custom-platform-tool",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/visual-tool/*",
    "packages/application-platform",
    "packages/web-server",
    "packages/dsl/*"
  ],
  "scripts": {
    "start:tool": "cd ./packages/visual-tool/app; npm start",
    "start:server": "minictl build",
    "prettier:base": "prettier --parser typescript",
    "format": "npm run prettier:base -- --write \"src/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "dependencies": {
  },
  "author": "SANGET",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/SANGET/react-app-seed.git"
  }
}
