export const assertPropsKey = (key: string, originKeys: string[], useKeys: string[]) => {
  // if (process.env.NODE_ENV === 'development') {
  //   if (originKeys.length !== useKeys.length) {
  //     console.group(key);
  //     console.warn(`组件\${${key}}props传入有误;`);
  //     console.warn('originKeys: ', originKeys);
  //     console.warn('useKeys: ', useKeys);
  //     console.groupEnd();
  //   }
  // }
};
